#include <stdio.h>
/**
 * main - Entry point
 *
 * description: 'prints single digit numbers starting from 0'
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; m <= 9; n++)
	{
	printf("%d", n);
	}
printf("\n");
return (0);
}
